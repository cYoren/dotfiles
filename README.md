# Gus's dotfiles

Current daily driver: **Omarchy** (Arch + Hyprland) on a Dell G15 5525 (AMD 680M iGPU drives the panel, RTX 3050 owns HDMI).

The star of the show is the **animated ASCII wallpaper**: the live [Asciify Fluid](https://asciify.org/docs/backgrounds/fluid) canvas pinned to the Wayland background layer. It reacts to the pointer, renders on the GPU, and runs as its own process so it can never take the shell down with it.

## Layout

Paths mirror `$HOME`, so `rsync` (or `stow .`) drops everything in place.

| Path | What |
|------|------|
| `.config/hypr/` | Hyprland (Omarchy Lua config) + hyprsunset. `hyprland.lua` forces VA-API/GLX onto the AMD iGPU |
| `.config/omarchy/` | Omarchy shell layout, theme (Catppuccin Mocha, generated by Aether), branding, hooks |
| `.config/{alacritty,ghostty,kitty,foot}/` | Terminals |
| `.config/{nvim,tmux,btop,starship.toml,git/config}` | Editor & shell tooling (nvim is LazyVim) |
| `.local/share/asciify-fluid/wallpaper.py` | The wallpaper: WebKitGTK window + `liblayer-shell-preload.so` → background layer |
| `.local/bin/asciify-fluid-wallpaper.sh` | Alternative Chromium-based launcher for the same wallpaper |
| `.config/systemd/user/asciify-fluid-wallpaper.service` | Starts the wallpaper with the graphical session |
| `etc/modprobe.d/nvidia.conf` | NVIDIA runtime-PM / S0ix options that make suspend and the hybrid GPU behave |
| `manifests/` | `pacman -Qqe` and AUR package lists |
| `scripts/restore.sh` | rsync into `$HOME` + enable the wallpaper service |
| `archive/` | Previous setups, kept intact (see below) |

## Restore

```bash
git clone https://github.com/cYoren/dotfiles ~/Documents/dotfiles
cd ~/Documents/dotfiles
sudo pacman -S --needed - < manifests/pacman-explicit.txt
yay -S --needed - < manifests/aur.txt
./scripts/restore.sh
```

The wallpaper needs `webkit2gtk-4.1`, `python-gobject` and `gtk4-layer-shell` (for `liblayer-shell-preload.so`); all are in the manifest.

Kernel cmdline extras on this laptop (set in `/etc/default/limine`, then `limine-update`):
`acpi_backlight=native amdgpu.dcdebugmask=0x10`

## Archive

- `archive/debian-plasma-hyprland-nix/` — the previous Debian machine: KDE Plasma + Hyprland via Nix Home Manager, apt/flatpak/nix manifests, and the first generation of ASCII wallpapers (rendered mp4 spirals + the Python generator). Its own README explains the restore flow.
